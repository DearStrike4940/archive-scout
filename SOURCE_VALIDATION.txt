Archive Scout 1.0.7.1 source validation
Date: 2026-09-18
Baseline: uploaded archive-scout-main(2).zip, commit 405f5eebdb4fbb165c9a5c718549848065cc80a6

Executed on Linux / Python 3.12
- Complete unittest discovery: 266 tests run, 265 passed, 1 skipped, 0 failures/errors.
- The skipped test needs a graphical display (test_v3_ui_theme).
- Python compilation: archive_scout, tests and scripts passed.
- git diff --check passed.
- 100,000-CDX-row offline benchmark completed; 100,000 inserts, zero duplicate work and zero writes on unchanged re-upsert.

Network validation is offline/local
- Real local HTTP server tests cover valid ranges, ignored ranges, mismatched offsets and rejected ranges through httpx, urllib3 and curl.
- Mocked regressions cover proxy/environment policy, backend initialization failures, preserved partial bytes and bounded page submission.
- CDX tests reject incomplete JSON rows and preserve literal nested URL/resume-key characters after a query round trip.
- Input tests cover normal URLs, wholly encoded/double-encoded URLs and copied CDX request URLs.

Other focused regressions
- Report controls persist, Reports is visible in Simple mode in the requested position, and output changes preserve historical scan data.
- Report match ordering/limits are consistent across ranked output and URL reports.
- Raw text .txt files retain legacy header encodings; binary signatures override incorrect text MIME.
- Hitlist interruption cannot advance past uncommitted results; binary bodies do not count as checked text.
- Worker startup failure restores controls; full-run rate-limit pauses preserve their original exception.
- Failing local scans remain retryable rather than looping indefinitely.
- Existing capture paths and portable filename collisions preserve separate captures.
- Unsupported newer project schemas are not migrated as legacy files.

Commands (after installing requirements-runtime.txt)
python -m unittest discover -s tests -p 'test_*.py'
python -m compileall -q archive_scout tests scripts
python scripts/benchmark_offline.py --cdx-rows 100000 --result-rows 1000 --keyword-score-runs 20 --output benchmark.json

Test process proxy variables were cleared so local-server fixtures did not use this environment's outbound proxy. Dedicated proxy-policy tests use controlled environment fixtures.

Limits
- No live Wayback availability/throughput test was performed. Eight request starts/second remains the configured replay ceiling, not a guaranteed completion rate.
- No native Windows/macOS packaged builds or interactive GUI display check was run here. Use the existing CI/build matrix and packaged smoke tests before release.
- The friend's exact failing CDX request was not supplied, so its specific cause is unconfirmed.
- Source patch only: no release/tag published and no credentials or downloaded project data included.
